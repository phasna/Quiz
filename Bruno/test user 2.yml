info:
  name: test user
  type: http
  seq: 1

http:
  method: PATCH
  url: http://localhost:3000/api/users/1/avatar
  body:
    type: json
    data: '{ "mediaId": 1 }'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
