meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: http://localhost:3000/api/auth/login
  body: json
  auth: none
}

headers {
  content-type: application/json
}

body:json {
  {
    "username": "phasna",
    "password": "sonmotdepasse123"
  }
}

script:post-response {
  const body = res.getBody();
  if (body?.token) {
    bru.setVar("token", body.token);
  }
}
